# Port Status

Agents update this file at the end of every work package.

## Infrastructure work packages

| WP | State | Notes |
|---|---|---|
| 0.1 Sync with upstream and restructure | done | Merged upstream `ComposersDesktop/CDP8@28bc42c` (15 commits: same Linux/GCC fixes our patch script applied, `specfnu` memory-allocation fixes, `aaio` relicensed to LGPL 2.1). Removed `docker-cdp8/fix-linux-compat.sh`. Moved `dev/`, `include/`, `cmake/`, `CMakeLists.txt`, `building.txt` to `legacy/`. Removed the stale `dev/sfsys` (an unbuilt duplicate of `newsfsys`). Also removed the top-level `include/` copies of `sfsys.h`, `props.h`, `osbind.h`, `chanmask.h` and `cdplib.h`, since `dev/newinclude/` already shadowed them. Moved the one non-duplicate header, `aaio.h`, into `dev/newinclude/`. Fixed `CPACK_RESOURCE_FILE_LICENSE` (broke `cmake` configure after the move). Updated Dockerfile/README build paths to `cmake ../legacy` and `legacy/NewRelease`. Added the workspace `Cargo.toml` (no members yet) and `templates/rust-file-header.txt`. Verified from a fresh clone of the commit alone, rebuilt in Docker: same 220 executables, same 3 warnings, 0 errors, core commands (`synth`, `sndinfo`, `pvoc`, `modify`) run and match pre-move output. |
| 0.2 Capture the specification | in progress | `tools/extract-specs/dump-usage.sh` captured usage text for all 220 programs and 368 sub-commands into `spec/usage/` (one empty file, `newtex.txt`, from a pre-existing stdin-hang bug). `tools/extract-specs/parstruct.py` extracted `legacy/dev/cdp2k/parstruct.c` into `spec/commands/_raw/parstruct.json` (299 processes, 645 process/mode entries), validated by hand against five processes. Remaining, deferred to individual program work packages rather than a bulk script: extracting `legacy/dev/cdparams/parnames.c` (irregular structure, mixes `case()` with `if(mode==...)` conditionals) and cross-referencing each group's `ap_*.c` dispatch table to map process/mode symbols to the CLI names users type. `legacy/dev/cdp2k/validate.c`, despite its name, holds runtime validity-flag logic, not a name table, so it does not help with that mapping. |
| 0.3 Continuous integration | done | Added `.github/workflows/ci.yml`: a `rust` job (fmt/clippy/test on Linux and macOS, skipped while the workspace has zero members), a `legacy-build` job (Docker build, binary-count and smoke-test checks, saves the image as an artifact), and a `golden-tests` job (placeholder until `tools/oracle` exists per WP-1.7). Verified the member-count skip logic locally with a real Rust toolchain: an empty workspace makes `cargo fmt`, `clippy`, and `test` all hard-error rather than succeed trivially, confirmed with a throwaway probe crate that the non-empty path works once a crate exists. First PR run (#1) had a Windows job in the matrix that failed: its `run:` step used bash syntax, but Windows runners default to PowerShell. The project owner decided to drop Windows from the target platforms (see PLAN.md D10) rather than fix the shell mismatch, so the matrix now runs Linux and macOS only. |

## Phase 1: foundation crates

| WP | State | Notes |
|---|---|---|
| 1.1 `cdp-sf` | in progress | Two slices so far. First slice: reads and writes plain WAVE files (`fmt `+`data`+`PEAK`), 16-bit PCM and 32-bit float only. Reads (does not yet write) the `cue `/`LIST`/`adtl`/`note`/`sfif` named-property block. Every on-disk layout (fmt chunk incl. WAVE_FORMAT_EXTENSIBLE sub-format GUIDs, PEAK chunk, property block hex encoding) was read from `legacy/dev/newsfsys/sfsys.c`/`props.c`, not guessed. Verified three ways: 17 unit tests (property codec, PEAK codec, fmt parsing, write/read round trip); 2 integration tests against real files with expected values computed independently (Python, not this crate's own code) and cross-checked against `legacy` `sndinfo props`; and a scan of all 553 `.wav` files in `docs/manual/sounds` (100% open and decode without error), with a random sample of 10 cross-checked field-by-field against `legacy` `sndinfo props` (all matched). Second slice: reads AIFF and AIFC files (`crates/cdp-sf/src/aiff.rs`, plus AIFF-specific additions to `props.rs`/`reader.rs`). Same `SoundFile` API as WAVE. AIFF/AIFC's `COMM`/`SSND`/`PEAK`/`APPL` chunks are big-endian. The crate normalises sample bytes to little-endian once, at open time, so every other part of the crate stays format-agnostic. Ported the 80-bit IEEE extended-float sample rate decoder (`legacy/dev/newsfsys/ieee80.c`). Hand-verified it against a real file's raw bytes with `xxd` and an independent Python `struct.unpack`, not against this crate's own code. All 120 real AIFF files under `docs/manual/sounds` are plain AIFF (16-bit PCM). None are AIFC. So the AIFC branch (float and packed 24-in-32 samples, the QuickTime 16-bit-declared-as-32 quirk) is unit-tested only, against hand-built fixtures, not corpus- or live-verified. The one plain-AIFF corpus file this crate's own tests inspect field by field (`ws2/tsw1-2nd.aiff`) had its `PEAK` and `DATE` values decoded once by hand in Python from the raw bytes before being written into the test. This is the same independent-oracle discipline the WAVE tests use. All 120 AIFF files open and decode without error. 10 are cross-checked against `legacy` `sndinfo props` (channels, sample rate, sample count) via the `cdp8-postmerge` Docker image. The crate has 33 unit and integration tests after that slice. Third slice: detects the CDP-derived analysis-family file kinds (`crates/cdp-sf/src/props.rs`'s `FileKind`/`detect_file_kind`) that live inside an ordinary float32 WAVE or AIFC file's property block: envelope, pitch, transposition, formant and generic analysis files. Ported `sf_headread`/`snd_headread`'s checksum-based detection from `legacy/dev/newsfsys/props.c` exactly, including its quirks: the five analysis properties (`original sampsize`, `original sample rate`, `arate`, `analwinlen`, `decfactor`) are summed into one checksum, and a zero checksum means plain WAVE or envelope (checked separately via an `"is an envelope"` marker) rather than analysis; a nonzero checksum means pitch, transposition or formant (in that priority order, by which marker property is present) or, with no marker, generic analysis. Pitch, transposition and formant files must have exactly one channel and carry an extra `"orig channels"` property recording their parent analysis file's channel count; formant files also carry `specenvcnt`. Every property name and the exact detection logic were confirmed against `strings` output of real corpus files, not guessed. No formant-file example exists anywhere in this repository's corpus, so that one branch is unit-tested against a hand-built fixture only, the same documented gap as the AIFC branch in the previous slice. Verified: all 91 real analysis-family files (`.ana`, `.evl`, `.frq`, `.trn`) under `docs/manual` open and classify as non-`Wave`, and two of them (`capm.ana`, an analysis file, and `crklenv.evl`, an envelope file) have every field cross-checked against `legacy` `sndinfo props`, with the raw property bytes independently decoded in Python. The crate had 61 unit and integration tests after that slice. Fourth slice: writes the WAVE property block (`crates/cdp-sf/src/writer.rs`'s `WriteSpec::properties`/`write_cue_chunk`). Ported `wrwavhdr98`'s exact chunk layout from `legacy/dev/newsfsys/sfsys.c`: an optional `cue ` chunk (one fixed cue point naming the `sfif` property inside the `data` chunk), then `LIST`/`adtl`/`note`/`sfif` holding `PropertyBlock::encode_padded`'s output. `write_cue_chunk` is a caller choice, not inferred from sample type, because real files differ: `docs/manual/sounds/marimba.wav` (an ordinary sound file) has a `cue ` chunk, but `docs/manual/data/capm.ana` (an analysis file) does not, matching a legacy comment that analysis files skip it. `PropertyBlock::encode_padded` pads with `'\n'` bytes to the legacy 2000-byte reservation, confirmed against the real padding bytes in `marimba.wav` (`0x0A` repeating, not zero, since `writeprops`'s own padding loop writes `'\n'`). Verified: unit tests confirm the exact chunk order for both the with-cue and without-cue cases against those two real files' own layout, and a file this crate's own writer produced (with a `DATE` property and a `cue` chunk) was read back correctly by a live `legacy` `sndinfo props` run through the `cdp8-postmerge` Docker image, decoding the same date `marimba.wav` itself does. The crate has 74 unit and integration tests in total. Not yet done: PVOC-EX, WAVE_FORMAT_EXTENSIBLE multichannel decoding, writing AIFF/AIFC, and sample types other than 16-bit PCM and float32 (`Int32`/`Int2424`/`Int2432`/`Int2024`/`Masked` are detected in `FmtInfo` but not yet decoded to samples). |
| 1.2 `cdp-data` | in progress | Three slices so far. Breakpoint files: parsing, range validation with the exact legacy error text, and evaluation. Two evaluators: `interpolate` (binary search, always correct regardless of query order) and `interpolate_from_cursor` (a faithful port of `read_value_from_brktable` in `legacy/dev/cdp2k/tklib3.c`, the function actually used everywhere -- not the similarly-named `interp_val` in `readdata.c`, which turned out to be dead code, never called from any program in this codebase, discovered by checking call sites before porting it). The number tokenizer (`get_float_from_within_string` in `tklib1.c`) is ported byte-for-byte, including real quirks: no scientific notation, and one invalid character invalidates the whole token and abandons the rest of the line. One of these quirks was cross-checked against a real run of `legacy` `modify loudness` with a crafted file containing `1e-5`, which fails with "Data not paired correctly" -- confirming an initial, incorrect assumption (that partial digits before the invalid character survive) was wrong, before it shipped. Also ported: both real dB-to-gain conversions in the codebase, `convert_dB_at_or_below_zero_to_gain` (attenuation only, errors on a positive value) and the more general `dbtogain` (allows boost). Mix files: parsing and validation of the per-line grammar in `legacy/dev/submix/setupmix.c` (`get_mixdata_in_line`, `finalise_and_check_mixdata_in_line`, and the four `check_*` field scanners). This covers all three documented line lengths (4, 5, or 7 words), the dB-aware level syntax, and the `L`/`C`/`R` pan shorthand. Note: `check_right_pan`'s `C` means `0.5`, not `0.0`. This is an asymmetry with `check_left_pan`, ported as written, not a bug. A new tokenizer path (`scan_c_double_prefix`/`scan_c_int_prefix`) mimics raw `sscanf("%lf"/"%d", ...)`. Unlike the breakpoint tokenizer, this path does accept scientific notation. A real run of `legacy` `submix mix` (via the `cdp8-postmerge` Docker image) checked this path, and checked the 4-word stereo-line derivation (`right_level = left_level`, hard left/right pan), on a generated stereo file and on the repository's own `docs/manual/data/simplemix.mix`. A live run could not check the field-level error messages (ported from `setupmix.c`) the same way. `legacy`'s own file-type auto-detection rejects a malformed mixfile earlier, with a generic error, before `setupmix.c`'s checks run at all. See the module doc in `crates/cdp-data/src/mix.rs` for detail. One legacy bug is documented in `docs/migration/LEGACY-BUGS.md`. In the C code, a 4-word line with `chans` other than 1 or 2 leaves data uninitialised. This port reports a data error instead. Not yet done for mix files: opening the referenced sound files, `-s`/`-e` start/end windowing, buffer allocation, and per-channel gain-scale computation (`d_assign_scaling`). These belong to the `submix` program itself (WP-2.7). Texture note-data files (`crates/cdp-data/src/notedata.rs`): the `notedata` grammar shared by every `texture` sub-command (`legacy/dev/texture/texprepro.c`), a sample-pitches line followed by zero or more `#N`-headed motifs of five-field note lines (`time instr_no pitch amp dur`). This is grammar only. The motif-count-vs-expected check depends on which `texture` sub-command and mode is asking, so it is exposed as `NoteDataFile::check_motif_count`, parameterised, for the future `texture` program WP-3.15 to call. The pitch/amplitude MIDI-range and minimum-duration checks in the C source are likewise conditional on that mode, and were left out for the same reason. The `instr_no` field is parsed and kept for fidelity, even though legacy itself computes it and never stores it. Verified against 13 real corpus files under `docs/manual/data` (all of `ndf*.txt`, `rhythtempl.txt`, `tmotifsinhf.txt`), all parsing correctly. Also verified with 8 separate live cross-checks against `legacy` `texture simple`/`texture motifs`, run through the `cdp8-postmerge` image, which reaches notedata parsing before any audio synthesis. Two of these were real corpus files running to completion. Six were deliberately malformed files, each reproducing the exact legacy error text character for character (insufficient pitches, missing `#`, non-digit/zero datalength, a missing note line at EOF, reverse note-time order, and the "at least" motif-count case). The crate has 74 unit and integration tests in total. Not yet done overall: tuning files and plain number lists. Investigation found these are not, in fact, one shared cdp2k format. `spectune`, `newscales` and `tunevary` each hand-roll their own copy of the same low-level read loop, each with its own pairing rules and error text. Porting them belongs to those programs' own future WPs (Phase 3/4), building on the tokenizer already in `crates/cdp-data/src/tokenizer.rs`, rather than to a new cdp-data module. |
| 1.5 `cdp-core`/`cdp-cli` | in progress | One slice. Added three crates: `cdp-core` (the exit-status lifecycle), `cdp-cli` (the `cdp` binary and the `synth` legacy-name launcher), and `cdp-programs` (brought forward from its later phase because this WP's done-when needs one real program wired end to end -- see its own module doc for why). `cdp-core::error`: `ExitCategory` (`legacy/dev/include/globcon.h`'s `USER_ERROR`/`DATA_ERROR`/`USAGE_ONLY`/etc.), `CdpError`, and `report_and_exit`, which reproduces `legacy/dev/cdp2k/mainfuncs.c`'s `print_messages_and_close_sndfiles` byte for byte: the per-category header line (or none, for `USAGE_ONLY`), `splice_multiline_string`'s per-line `"ERROR: "` prefix, and the unconditional trailing `"\n\n"` printed on every run whether it succeeds or fails. Confirmed live that every one of the 27 legacy `main()` functions returns only `FAILED`(`-1`)/`SUCCEEDED`(`0`) regardless of which category triggered the failure, so `report_and_exit` always exits `0` or `-1` (`255` on Linux/macOS, confirmed live) -- `ExitCategory` only controls what gets printed. `From<cdp_params::ParamsError>` maps every variant to its real legacy category, each confirmed with its own live `legacy` run via `cdp8-postmerge` (documented per arm in `crates/cdp-core/src/error.rs`) by reading `legacy/dev/cdp2k/readdata.c` end to end for which of `get_options`/`get_variant_no`/`get_params` returns which category, since the printed shape (header or not) is not guessable from the message text alone -- e.g. `"Duplicate option"` (from `get_options`) is `USAGE_ONLY` but `"Duplicate flag"` (from `get_variant_no`) is `USER_ERROR`, despite reading like the same kind of error. One documented gap: `ParamsError::UnknownParameter` is produced by two different legacy call sites with two different categories, but only one is reachable by any `CommandSpec` shipped so far (see the impl's doc). Found and fixed a real bug in already-merged WP-1.3 code while verifying this: `CommandSpec::synth_wave`'s `unequal_sndfile` was `false`; live `synth wave 1` (mode only, no other arguments) prints `"Insufficient parameters on command line."`, not `"Insufficient cmdline parameters."`, so it must be `true` (`synth wave` really is `UNEQUAL_SNDFILE` in `legacy/dev/synth/ap_synthesis.c`) -- fixed, with a new regression test; every other confirmed case for that command was already correct regardless of this field (see the fix's own doc comment for why). Extended `cdp-params` with `parse_mode`/`ParamsError::{CannotReadModeOfProgram, ModeOutOfRange}`, porting `get_mode_from_cmdline` (`legacy/dev/cdp2k/tklib3.c`), generic across every group program: confirmed live, including the `sscanf("%d")` truncation quirk (`synth wave 3.5 ...` and `synth wave 3abc ...` both run mode 3 to completion, not an error). `cdp-programs::synth::wave` ports `legacy/dev/synth/wave.c`'s `gentable`/`gen_wave` in full: all four waveforms (sine, square, sawtooth, ramp), the 256-frame linear start/end splice, and -- not skipped as a simplification -- the 10-millisecond-block envelope stepping `update_wave_params` uses when `freq`/`amp` are breakpoint files rather than plain numbers (linear for amplitude, logarithmic for frequency, via `cdp_data::BreakpointTable::interpolate_from_cursor`, the same cursor-based evaluator `cdp-data`'s own module doc already identified as the one legacy actually uses everywhere). Chosen as the one program to wire up because it needs no FFT or phase vocoder (`cdp-dsp`, WP-1.4, not built yet). `cdp-cli` builds two binaries from one shared `cdp_cli` lib: `cdp` (`cdp synth wave 1 outfile.wav 44100 1 1.0 440`) and `synth` (a legacy-name launcher, `synth wave 1 outfile.wav ...`, plus `synth --version` printing the real legacy `synth` version string `7.1.1`, confirmed live -- each launcher will need its own confirmed version string as more are added, there is no shared "legacy version"). `cdp --version`/`cdp --help` are new, with no legacy equivalent (confirmed live that legacy has no `--help` anywhere). Verified: `cargo test --workspace` (67 `cdp-params` unit tests, up from 61 -- the mode-parsing tests and the `synth_wave` regression test; 9 new `cdp-programs` unit tests covering `gentable`'s per-mode endpoints, frame-count, the `-a` amplitude scale, a breakpoint `freq`, and the splice-length guard); `cargo fmt`/`cargo clippy --workspace --all-targets -- -D warnings` clean. Live, via `cdp8-postmerge`: byte-for-byte `diff` of this crate's stdout against real `legacy` for a bad mode number, an out-of-range parameter, a missing breakpoint file, insufficient parameters, and `synth wave`'s own bare-subcommand usage text (a literal fixture, `wave::USAGE`, captured from a real run -- general usage-text generation from a `CommandSpec` remains out of scope, per `cdp-params`'s own module doc) -- all matched exactly, including exit codes. Sample-level: generated real `.wav` files with both `cdp` and `legacy` `synth wave` for all four modes, mono and stereo, with `-a`/`-t` flags, and with a breakpoint `freq` file, then compared every sample with an independent Python decoder (not this crate's own reader) -- every run matched exactly on sample count and channel count, with a maximum absolute difference of 1 (out of 32767, well inside D4's tolerance), consistent with a rounding-mode difference in `cdp-sf`'s existing PCM16 encoder, not a synthesis error. **PLAN.md's WP-1.5 done-when criterion ("`cdp synth wave ...`, `synth wave ...` through the launcher, `--version`, and `--help` work and exit codes match legacy") is met** for the one program wired up. Left deliberately out of scope: `cdp-core::Context` (`docs/migration/PLAN.md`'s "the parsed command, the open inputs and outputs, the parameter tables, and a progress reporter" struct) -- with only one program wired up there is no second real case to confirm that shape against, the same reasoning WP-1.3 gives for deferring its general TOML loader; `cdp-programs::synth::wave` holds its own state directly instead. Progress reporting (`legacy`'s `"\rN min S.SS sec"`, from `legacy/dev/cdp2k/writedata.c`'s `display_virtual_time`) is not implemented -- it depends on buffer-chunked writing this crate's writer does not do (it buffers the whole file), and WP-1.5's done-when does not require matching progress text, only exit codes. Any program name/sub-command other than `synth wave` reports a plain "not implemented yet" message rather than legacy's own usage listing (confirmed live to differ from `synth`'s bare-name usage, e.g. `synth noise`), since this crate does not yet know the other legacy program/sub-command names -- that comes from `spec/usage/` (WP-0.2) one program at a time as each gets wired up here. The `-f`-before-outfile convention (`legacy/dev/cdp2k/mainfuncs.c`'s `look_for_float_flag`, requesting float rather than 16-bit output) is not implemented; `synth wave` output is always 16-bit PCM. **Next**: WP-1.4 (`cdp-dsp`) and WP-1.6 (`cdp-spectral`) remain open per PLAN.md's phase sequencing; a second program wired into `cdp-programs`/`cdp-cli` (ideally one needing more of `dz`'s state) would be the natural next step to confirm `Context`'s real shape before building it. |
| 1.3 `cdp-params` | in progress | First slice: a hand-written `CommandSpec` for `modify loudness` mode 1 (`LOUDNESS_GAIN`: `modify loudness 1 infile outfile gain`) and a positional-argument parser for it. Investigation found that the general "TOML spec from parstruct.json" loader planned in PLAN.md is blocked on WP-0.2's still-unfinished cross-reference of `parnames.c` and each group's `ap_*.c` dispatch table (needed to map process/mode symbols to CLI names), so this slice instead hand-derives one real command's parameter type and range straight from `legacy/dev/cdp2k/parstruct.c` (`set_param_data(ap,0,2,1,"D0")`) and `tklib1.c`'s `set_param_ranges` (`ap->lo[LOUD_GAIN]=0.0`, `ap->hi[LOUD_GAIN]=MAXSHORT`). Implements two of the five parameter types PLAN.md names, `DoubleOrBreakpoint` and `File`: a `DoubleOrBreakpoint` token that fails to parse as a plain number is retried as a breakpoint filename via `cdp_data::BreakpointTable::from_file`, reusing its parsing, range-checking and error text directly. Whether a separate non-breakpoint-fallback `Double` type is real, and what `Int`/`IntOrBreakpoint` look like, is not yet confirmed against any real command, so those three are deliberately left out. Verified against 8 live runs of `legacy` `modify loudness` via the `cdp8-postmerge` Docker image: the two-tier argument-count check (`"Insufficient cmdline parameters."` when a file argument is missing, `"Insufficient parameters on command line."`/`"Too many parameters on command line."` for the numeric parameter count), the generic infile-open check (`"Can't open file %s to read data."`), range checking (`"Parameter[1] Value (...) out of range (0.000000 to 32767.000000)"`), and the breakpoint-file fallback (reproducing `cdp_data`'s own `"Can't open brkpntfile abc to read data."` for an unparseable token) all match exactly. The crate had 9 unit tests after that slice. Second slice: modes 3 and 4 (`LOUDNESS_NORM`/`LOUDNESS_SET`: `modify loudness 3`/`4 infile outfile [-llevel]`), which have no required positional parameters at all, only one optional `-l<level>` flag. This confirmed a real, plain `Double` parameter type exists after all (no breakpoint-file fallback), distinguished in `parstruct.c` by a lower-case type letter (`opt_list = "d"`) versus mode 1's upper-case `"D0"`. It also surfaced a genuine legacy inconsistency, ported as observed rather than smoothed over: an out-of-range `-l` value reports `"Parameter[1]"`, but an unparseable one reports `"Cannot read parameter 2"` for the very same parameter, evidently from two separate, differently-numbered code paths in `legacy/dev/cdp2k/tklib1.c`, so `ParamType::Double` carries that second number as a literal, verified fact rather than something this crate computes. Also added: `OptionFlag` (one `-<letter><value>` optional flag, value attached with no space) and a distinct `"Unknown parameter '%s'"` error for a mode whose parameter list is empty, separate from `"Too many parameters on command line."` (used when the parameter list is non-empty) -- both confirmed live to be genuinely different messages for what looks like the same underlying "extra word" situation. Verified against a further 7 live runs of `legacy` `modify loudness` 3 and 4: no flag, an attached value, an out-of-range value, an unparseable value, a bare flag with no value, a space-separated flag and value (also "no value", since legacy never joins the next word to it), an unrecognised flag letter, and a plain leftover word all match exactly, independently confirmed for both modes. The crate has 18 unit tests in total. Third slice: `pvoc anal` (`PVOC_ANAL`). All three modes share one `CommandSpec`, since `parstruct.c` and `tklib1.c` both handle `PVOC_ANAL` in one `case`, not per mode. Command shape: `pvoc anal mode infile outfile [-cpoints] [-ooverlap]`. This confirmed a real `Int` parameter type at last. `legacy/dev/cdp2k/tklib3.c`'s `mark_parameter_types` reads one of four letters (`i`, `I`, `d`, `D`) off the same parameter-list string, and `parstruct.c`'s `set_vflgs(ap,"co",2,"ii","",0,0,"")` uses lower-case `i` for both `-c` and `-o`. `Int` parses identically to `Double` (the same `%lf`-style tokenizer and range check) and differs only in the stored value. A live run of `-c512.9` produced an analysis file reflecting channel count `512`. This confirms legacy's `(int)` cast truncates toward zero, rather than rejecting a fractional token or rounding it, so `ParamValue::Integer` applies that same truncation (`value as i64`) to the already range-checked double. This slice also found and fixed a real bug in the existing parser, not specific to `pvoc anal`. Repeating the same flag letter (`-c512 -c99999`) silently let the second value overwrite the first. Legacy's `get_options` (`legacy/dev/cdp2k/readdata.c`) rejects this instead, with `"Duplicate option %c used on command line"`, checked before the second value is even parsed. This is confirmed live for `pvoc anal` and, retroactively, for the already-shipped `modify loudness 3`/`4` `-l` flag. The fix applies generically, to every command with optional flags, not just this one. Verified against 9 further live runs of `pvoc anal`. No flags, and both flags in either order, parse correctly. Each flag out of range reports `"Parameter[1]"` for `-c` and `"Parameter[2]"` for `-o`, and these errors report in command-line order rather than by paramno (`-c1 -o5` and `-o5 -c1` report different parameters first). Each flag unparseable reports `"Cannot read parameter 1/2 [abc]: brkpnt_files not permitted."` A bare flag with no value, an unrecognised flag, and a leftover non-flag token each report their own distinct error. The duplicate-flag case is confirmed too, including that a bare second occurrence (`-c512 -c`) still reports the missing-value error ahead of the duplicate check, matching legacy's check ordering exactly. The crate has 32 unit tests in total. Fourth slice: `distort repeat` (`DISTORT_RPT`). Command shape: `distort repeat infile outfile multiplier [-ccyclecnt] [-sskipcycles]`. This is the first command with a required positional parameter and flags together, and the first with any variant flags at all. `multiplier` and `-c` are `IntOrBreakpoint` (upper-case `I` in `parstruct.c`, the fifth and last of PLAN.md's named parameter types), confirmed live both by a real breakpoint file for `multiplier` and by the same truncation-not-rounding behavior `Int` already showed. `-s` is a plain `Int` variant. Legacy scans flags in two ordered phases: `get_options` first, consuming a contiguous prefix of known option letters and stopping without error at the first it does not recognize, then `get_variants_and_flags` scans everything left over as variants. This makes flags and variants order-dependent. A live run of `-s1 -c2` (variant before option) fails with `"option flag -c out of order on cmdline."`, naming the option letter, not the variant already read, since option scanning has already permanently stopped by that point. Paramno numbering is confirmed to be global and sequential across params, then flags, then variants, in declaration order, not restarting at 1 for the flags: `multiplier` is `Parameter[1]`, `-c` is `Parameter[2]`, `-s` is `Parameter[3]`. This command is also `UNEQUAL_SNDFILE`-classified in `legacy/dev/distort/ap_distort.c`'s `setup_process_logic`, unlike every command ported so far. This does not change its practical infile count, which is still exactly one, but it does change the error text when the command line is too short even for the infile, outfile and multiplier combined: `"Insufficient parameters on command line."` instead of `"Insufficient cmdline parameters."`, confirmed live with both just an infile, and an infile plus outfile, given. The general `UNEQUAL_SNDFILE` variable-infile-count algorithm (`count_infiles` in `legacy/dev/cdp2k/mainfuncs.c`) is not implemented, only this one confirmed consequence. Verified against 15 further live runs of `distort repeat`. A bare `multiplier`, and `multiplier` with both `-c` and `-s` in order, run to completion. `multiplier` and `-c` out of range or retried as a breakpoint file behave exactly as the existing `IntOrBreakpoint`-adjacent types already do. `-s` unparseable reports `"Cannot read parameter 3"`, matching its own paramno for this command, unlike `modify loudness`'s `-l` flag where the two numbers differ (confirming that mismatch is a per-parameter fact, not a general rule). A leftover non-flag token after all three are given reports `"Unknown parameter 'extra'"`, the same shape already confirmed for modes with no required params, now also confirmed with one present. Duplicating `-c` reports `"Duplicate option c ..."`; duplicating `-s` reports the variant-specific `"Duplicate flag s used on command line"`; a bare trailing `-s` reports `"variant parameter missing with flag -s"`, checked before the duplicate check, mirroring the option phase's own ordering (confirmed live with `-s1 -s`). An unrecognized `-z` during the variant phase reports `"Unknown variant flag -z"`. A pure boolean (no-value) variant flag, and the `"Unknown flag '-%c'"` message `get_variant_no` uses when a mode has variants but no options at all, are not implemented yet -- no real command confirmed live for either. The crate has 49 unit tests in total. Fifth slice: `synth wave` (`SYNTH_WAVE`, all four modes -- sine, square, sawtooth, ramp -- share one spec). Command shape: `synth wave mode outfile sr chans dur freq [-aamp] [-ttabsize]`, plus one boolean flag `-f` found only by reading `parstruct.c` directly. It is absent from the usage text entirely. This is the first command with more than one required parameter, and the first with a pure boolean variant flag, closing out the two items the fourth slice left open. `sr` and `chans` are plain `Int`, `dur` is plain `Double`, and `freq` is `DoubleOrBreakpoint`, all four confirmed at their own paramno (1 through 4) in order, continuing sequentially into `-a` (`DoubleOrBreakpoint`, paramno 5) and `-t` (plain `Int`, paramno 6). This is the first confirmation that paramno numbering stays sequential across multiple required parameters, not just from one required parameter into the flags after it. `-f` needs no paramno at all: confirmed live that `-f999` (a value attached to a boolean flag) behaves identically to a bare `-f`, silently discarding the `999`, since legacy's `get_variant_no` never inspects what follows a boolean variant's letter. `-f` still participates in the same order and duplicate checks as a value-carrying variant: given before `-a` it reports `"option flag -a out of order on cmdline."`, and given twice it reports `"Duplicate flag f used on command line."` This slice also surfaced a second, previously invisible "insufficient parameters" message. With every required parameter ported before this one having a count of exactly one, "zero given" and "some but not all given" were indistinguishable. With `synth wave`'s four, giving zero reports `"Insufficient parameters on command line."` as already known, but giving 1, 2, or 3 of the 4 (some but not all) instead reports `"Insufficient parameters on cmdline."` (no "command"), confirmed live for all three partial counts. `crate::spec::Variant` now models both shapes (`Value`, wrapping the existing `OptionFlag` shape, and `Boolean`), and `CommandSpec::variants` was changed to hold it instead of `OptionFlag` directly. The usage text's documented "recommended" values for `sr` (48000, 24000, 44100, 22050, 32000, or 16000) and `chans` (1, 2 or 4) are not the real range check, which is a much wider continuous interval (`sr`: 16000 to 192000; `chans`: 1 to 16) -- ported as observed in `tklib1.c`, not as documented. Verified against 13 further live runs of `synth wave`. A run with all four required params and no flags, and one with `-a`, `-t` and `-f` all given, both run to completion. Each of the four required params and both options out of range report their own paramno as above; `freq` retried as a real breakpoint file succeeds; `-a`'s breakpoint fallback failing on a nonexistent file reports the same `cdp_data` text every other breakpoint-capable type already uses; and a leftover non-flag token reports `"Unknown parameter 'extra'"`, the same shape already confirmed for every other flag-bearing mode. The crate has 61 unit tests in total. **PLAN.md's WP-1.3 done-when criterion ("the parser accepts every golden command line for `modify loudness` and `pvoc anal` and rejects the same inputs the legacy parser rejects with the same `ERROR:` text") is now met** by the first three slices; the fourth and fifth (`distort repeat`, `synth wave`) were not required by that criterion but closed out every remaining named parameter type and flag/variant mechanism PLAN.md's architecture section calls for, so the crate now models the full generic argument-parsing shape, not just these two commands. Left deliberately out of scope for WP-1.3 (see PLAN.md's own architecture section and this crate's module docs): the general TOML-based `CommandSpec` loader (blocked on WP-0.2, see the first slice's note), the `"Unknown flag '-%c'"` message for a mode with variants but no options at all (no real command confirmed live for it), usage-text formatting, and a `CommandSpec` for every other command (each future program's own WP hand-derives its own, following this crate's established pattern, since the TOML loader remains blocked). **Next per PLAN.md's phase sequencing** (section "Run Phase 1 with up to seven agents"): WP-1.5 (`cdp-core`/`cdp-cli`) was waiting on WP-1.3 and is now unblocked; WP-1.4 (`cdp-dsp`) and WP-1.6 (`cdp-spectral`) do not depend on WP-1.3 and can run in parallel with it or with each other. `cdp-sf` (WP-1.1) and `cdp-data` (WP-1.2) are also still "in progress" (see their own rows); unlike WP-1.3, their own stated done-when criteria are not yet fully met (WP-1.1's calls for an actual `sndinfo props` reimplementation printing matching values across the corpus, not yet built as its own thing even though the equivalent cross-checks already run inside `cdp-sf`'s own tests; WP-1.2's calls for every example file in `docs/manual/data` to parse, and the tuning-file formats are explicitly not parsed yet, deferred to their own future program WPs), but nothing about that blocks WP-1.4, 1.5, or 1.6 from starting. |
| 1.4 `cdp-dsp` | in progress | Two slices so far. First slice, `crates/cdp-dsp/src/windows.rs`, ports the window-construction block of `pvoc_process` (`legacy/dev/pv/pvoc.c` lines 53-73 and 272-358): `hamming()` verbatim, then `build_windows(m, n_chans, interp)`, which builds both the analysis and synthesis windows the phase vocoder's per-frame loop (not built yet) will use. Takes `M` (window length), `N` (`PVOC_CHANS`, always even) and `I`/`IO` (interpolation factor) as plain already-resolved parameters, since computing them from a command line's `-c`/`-o` flags and `PVOC_CONSTANT_A` belongs to the future `cdp-programs::pvoc` port that calls this module. All arithmetic mirrors the C code's exact `float`/`double` mixing (which multiplications happen before a value promotes to `double` for a trig call), using a `LEGACY_PI` constant equal to `globcon.h`'s literal `3.141592654`, not `std::f64::consts::PI`, since that is the actual value legacy's own math used. Found and ported, not smoothed over, a genuine legacy quirk: when `M > N` (overlap factors 0 and 1), the synthesis window's final scale factor reuses the *analysis* window's leftover normalisation factor (pvoc.c's `sum` variable, never reassigned between the analysis-window block and this point) rather than a fresh sum of the synthesis window itself, unlike the `M <= N` branch, which does compute a fresh sum -- see the module doc and `build_windows`'s own doc comment for the exact C lines. Verified against an independent Python oracle (hand-ported from the same C lines, using `struct.pack`/`unpack` to force float32 rounding at each cast point the C code has, not against this crate's own code): 8 unit tests cover all four overlap factors at `N=1024` (exercising both the `M<=N` and `M>N` branches for both windows), the `N=256`/`4096` boundary sizes named in PLAN.md's done-when, and the one case where `M` itself is odd (`N=514`, overlap 3, since `PVOC_CHANS` is forced even but `N/2` need not be). 11 unit tests total; `cargo fmt`/`cargo clippy --workspace --all-targets -- -D warnings` clean. Second slice: `crates/cdp-dsp/src/fft.rs` wraps `realfft`/`rustfft` (per PLAN.md's crate-responsibilities section) as the real-signal FFT `pvoc_process`'s per-frame loop needs, rather than porting `mxfft.c`'s mixed-radix algorithm itself. `RealFft::analyze`/`RealFft::resynthesize` reproduce the two call shapes `pvoc_process` uses (`fft_`+`reals_` forward with `isn=-2`, `reals_`+`fft_` inverse with `isn=2`) exactly, including one asymmetry between them: legacy's inverse pair already divides its result by the frame length internally, where `realfft`'s own inverse does not (confirmed against its own crate documentation: "RealFFT matches the behaviour of RustFFT and does not normalize the output of either forward or inverse FFT"), so `resynthesize` applies that `1/len` scale itself. This was confirmed, not assumed: a standalone C harness (not part of this crate) compiled `legacy/dev/pv/mxfft.c` alone -- it links against nothing but `globcon.h`'s `FINISHED`/`DATA_ERROR` constants and an `errstr` buffer, no dependency on the rest of `legacy`'s sound-file or option-parsing machinery -- and called `fft_`/`reals_` directly on an impulse, a DC signal, a single sinusoid at a known bin, a ramp (checked against its closed-form DFT), and an arbitrary signal, at `Nchans` 8, 16, 256, 1024 and 4096. This confirmed the forward pair's sign convention and scale match a standard unnormalized DFT (`X[k] = sum x[n] exp(-2*pi*i*k*n/len)`, `k` in `0..=len/2`) exactly, so `realfft` needs no conversion beyond the inverse's `1/len` scale, and that forward-then-inverse round-trips to the original signal with no extra factor (the ratio came back exactly `1.0`, not `len` or `1/len`). Every expected value in `fft.rs`'s 8 unit tests is that harness's literal output, not a value computed by this module or by `realfft`, the same not-derived-from-the-implementation-under-test discipline `windows.rs`'s Python oracle already established. Verified bin-for-bin at `Nchans=1024` (a real `pvoc` window size, not just the toy `Nchans=8` cases) against a signal with a known sinusoidal and DC content, and round-tripped at the `256`/`4096` window-size boundaries PLAN.md's done-when names. The crate has 19 unit tests in total; `cargo fmt`/`cargo clippy --workspace --all-targets -- -D warnings`/`cargo test --workspace` all clean. Third slice: `crates/cdp-dsp/src/pvoc.rs` ports `pvoc_process`'s analysis path (`legacy/dev/pv/pvoc.c` lines 429-573). Scope is `PVOC_ANAL_ONLY` (`pvoc anal`) only. Resynthesis stays out of scope. `analyze(samples, m, n_chans, d, sample_rate)` takes the whole input signal at once. It does not stream through the C code's fixed-size ring buffer (`ibuflen = 4*M`). Every index the ring buffer would read holds the same value a plain, zero-padded array (indexed by absolute sample time) holds, because `ibuflen` is always larger than one window's extent. So the ring never wraps onto data a window still needs. This is a confirmed-safe simplification, not an assumption. `frame_count(num_samples, anal_win_len, d)` reduces the C loop's `endsamp` (set only once EOF is detected mid-stream, from `nI + analWinLen - (D - Dd)`) to plain `num_samples` for a fully in-memory input. Two live `pvoc anal` runs via `cdp8-postmerge` confirmed this substitution before any bin value was checked: a freshly synthesised 4,410-sample file (`M=1024`, `D=128`) produced exactly 43 frames, and `docs/manual/sounds/capm.wav` (360,958 samples, same `M`/`D`) produced exactly 2,828 frames. The formula reproduces both counts exactly. `windowed_frame` ports the windowing-and-circular-fold block: it multiplies each of the `2*analWinLen+1` input samples around a frame's center time by the analysis window, then folds the result into an `n_chans`-point real buffer using the C code's `k = (nI+offset) mod n_chans` indexing. `convert_to_amp_freq` ports the magnitude/phase-unwrap block exactly, including the `real==0`/`mag==0` branches and the `+-PI`/`TWOPI` wrap. Every formula was first checked against an independent, from-scratch Python oracle (a hand-written radix-2 FFT and phase-unwrap port of the same C lines, not this module's code or `realfft`) run against `capm.wav`'s real 360,958 samples. That check compared bin-for-bin against `capm.ana`'s own real float data at three frames: an early silent frame, a frame partway through the file's real signal (first nonzero sample at 10,409), and the last, all-zero EOF-tail frame. This ran before any Rust was written. The crate's own tests repeat that same cross-check as part of `cargo test`, reading the real corpus files with the test's own from-scratch WAVE parser, not `cdp-sf`. `analyze_matches_real_legacy_capm_ana_corpus_file` matches all three frames within 1e-6 (silent and EOF frames) or 0.02 (the signal-bearing frame). That 0.02 gap matches float32-vs-`realfft`-internal rounding, not a formula error. The independent Python oracle showed the same order of difference at that frame. The crate has 22 unit tests in total. `cargo fmt`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` are all clean. Not yet done: resynthesis (the `PVOC_SYNTH`/overlap-add path), splices, interpolators, envelope followers, biquad/FIR filters, waveset detection, and the seedable `drand48`-compatible RNG. PLAN.md's WP-1.4 done-when (`pvoc anal`/`pvoc synth` round trips at D4 tolerance, all three analysis modes, four overlap values, window sizes 256-4096, mono and stereo) is still not met. This slice confirms the analysis math is correct in isolation. Nothing yet reads a real input file, calls `analyze`, or writes a real `.ana` output file end to end. `pvoc anal` is not wired into `cdp-programs`/`cdp-cli` at all yet. Fourth slice: `pvoc anal` wired end to end (`crates/cdp-programs/src/pvoc/anal.rs`, registered in `cdp-cli` as `cdp pvoc anal ...`). Scope: mode 1 (`STANDARD ANALYSIS`) and mono input only -- modes 2/3 write a different, smaller frame shape this crate's `analyze` does not produce, and multi-channel needs a per-channel loop not yet confirmed against `pvoc.c`; both report a plain `ProgramError`. `resolve_m_n_d` ports the `-c`/`-o`-to-`M`/`N`/`D` resolution left open by the third slice, reading `check_param_validity_and_consistency` (`ap_pvoc.c`) and `pvoc_preprocess` (`pvoc.c`) side by side to confirm both independently compute the same even-rounded channel count. This surfaced and fixed a real bug in the third slice's `convert_to_amp_freq`: legacy's `PI`/`TWOPI` are plain (double-precision) literals (`globcon.h`), so C's usual arithmetic conversions implicitly widen the `float angleDif` to `double` for the phase-wrap comparison and arithmetic; the existing Rust port instead rounded `PI`/`TWOPI` down to `f32` *before* comparing, shifting the wrap boundary. Found live: byte-for-byte comparison of this crate's output against `cdp8-postmerge`'s real `pvoc anal` (both the small synthesised `sine1.wav` case and the `capm.wav` corpus file, default and `-c512 -o2` flags) showed amplitude bit-exact everywhere but frequency occasionally off by a full `arate` step -- narrowed to bins where the sign of a near-zero imaginary part flips the quadrant-correction branch, then traced to the `f32`-vs-`double` `PI` rounding rather than the FFT itself (`cdp_dsp::fft`'s independently-confirmed forward/inverse pair was not in question). Fixed by keeping the wrap comparison and arithmetic in `f64`, narrowing to `f32` only at the end, matching the C casts exactly. After the fix, frequency matches to float32 noise (down to ~6e-5 Hz) for every bin with amplitude above the noise floor (confirmed by sweeping the amplitude threshold on the live comparison); only near-silent bins still occasionally diverge by a full `arate` step, a genuine, confirmed characteristic of any two independently-implemented FFTs disagreeing in sign on a near-zero value, not a formula bug -- phase is inherently unstable when magnitude is ~0. Verified: all header properties (`arate`, `analwinlen`, `decfactor`, `original sampsize`, `original sample rate`, channel count, sample rate) match a live `sndinfo props` run exactly; the bare `pvoc anal` usage-text fixture needed one fewer trailing blank line than the raw `spec/usage/pvoc/anal.txt` capture, since `ExitCategory::UsageOnly` already appends its own tail blank line (the same pattern `synth::wave::USAGE` already uses) -- confirmed by diffing against a live run. A new self-contained regression test (`analyze_end_to_end_matches_the_real_capm_ana_corpus_file`, no Docker at test time) opens the real `docs/manual/sounds/capm.wav`/`docs/manual/data/capm.ana` corpus pair via `cdp_sf::SoundFile` and checks the full written file -- header properties and, frame by frame, amplitude bit-exact plus frequency wherever amplitude clears the noise floor -- against the second-real analysis-file oracle this repository already carries. **Next**: modes 2/3 and multi-channel input remain open for a future slice; WP-1.6 (`cdp-spectral`) and the rest of Phase 1's still-`in progress` crates (`cdp-sf`, `cdp-data`, per their own rows) remain per PLAN.md's phase sequencing. |

## Phase 2: core time-domain and utility programs

| WP | State | Notes |
|---|---|---|
| 2.1 `sndinfo` | in progress | One slice: `sndinfo props infile` only, of the 17 sub-commands `sndinfo`/`sndreport` has (`props`, `len`, `lens`, `sumlen`, `timediff`, `smptime`, `timesmp`, `maxsamp`, `maxsamp2`, `loudchan`, `findhole`, `diff`, `chandiff`, `prntsnd`, `units`, `maxi`, `zcross`). `crates/cdp-programs/src/sndinfo/props.rs` ports `case(INFO_PROPS)` in `legacy/dev/sndinfo/compare.c`, plus its shared `prntprops` tail (the `PEAK`-chunk and `DATE`-property block every non-text-file case falls through to). Scope: `cdp_sf::FileKind::Wave`, `Analysis` and `Envelope` only -- the three kinds this repository's corpus has a real example of and `crates/cdp-sf/tests/oracle_fixtures.rs` already independently confirms field values for. `Pitch`/`Transposition`/`Formant` report a plain `ProgramError` (no real file of any of those three kinds exists anywhere in the corpus, so that branch would be confirmed against a hand-built fixture only); text-file/breakpoint-file/number-list detection (`is_a_textfile_type`) is out of scope entirely, since `cdp-sf`/`cdp-data` do not classify a file that way yet. `crates/cdp-programs/src/sndinfo/ctime.rs` is a from-scratch port of C's `ctime`/`asctime` (Howard Hinnant's `civil_from_days` day-count-to-calendar-date algorithm, not a year/month loop), needed because both the `DATE` property and the `PEAK` chunk timestamp print through it and no date/time crate is a workspace dependency (checked first). Verified against 10 independent `date -u -d @<ts>` runs spanning the real corpus timestamps, the epoch itself, a day boundary, and both an ordinary and a century (`%400`) leap year. This slice also extended `cdp-params`: `CommandSpec` gained a `has_outfile` field (`sndinfo props infile` takes no output filename at all, the first command ported so far that doesn't) and `ParsedCommand::outfile` changed from `String` to `Option<String>` accordingly; `CommandSpec::sndinfo_props()` reuses the existing empty-flags/empty-variants leftover-token check unchanged, confirmed live that a trailing word after `infile` reports `"Too many parameters on command line."`, not `"Unknown parameter"`. Also confirmed live and ported: the `"CDP Release 7.1 2016\n"` greeting legacy's `make_initial_cmdline_check` prints whenever raw `argc<4` -- true for `sndinfo props infile` (argc 3, greeting prints, success or failure alike, confirmed with a nonexistent infile too) but not `sndinfo props infile extra` (argc 4, no greeting, straight to the too-many-parameters error). Wired into `cdp-cli` as `cdp sndinfo props ...` and a new `sndinfo` legacy-name launcher binary (`sndinfo --version` prints `7.1.0`, `main.c`'s own `cdp_version`, confirmed live). Verified: 4 in-repo regression tests, each comparing this crate's `format_props` output against a real corpus file (`docs/manual/sounds/marimba.wav`, a SOUND file with no `PEAK` chunk but a `DATE` property; `docs/manual/sounds/ws2/tsw1-2nd.aiff`, a SOUND file with both a `PEAK` chunk and a `DATE` property; `docs/manual/data/capm.ana`, an ANALYSIS file; `docs/manual/data/crklenv.evl`, an ENVELOPE file, which -- ported as observed, not assumed -- has neither a `PEAK`-chunk check nor a `DATE`-property check at all, since legacy's own code returns immediately after the window-size line for envelope files). Live, via `cdp8-postmerge`: byte-for-byte `diff` of `cdp sndinfo props` against real `legacy` for all four corpus files above, a bare `sndinfo props`, a nonexistent infile, and an infile plus a trailing extra word -- all matched exactly, including the exact blank-line counts (`ctime`'s own embedded newline plus the format string's explicit one plus `report_and_exit`'s unconditional tail, confirmed to compound differently across the SOUND/ANALYSIS/ENVELOPE branches, not a fixed count). `cargo test --workspace`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. Second slice: `crates/cdp-programs/src/sndinfo/len.rs` ports `case(INFO_SFLEN)` in `compare.c` (`sndinfo len infile`, duration only). Same file-kind scope as the `props` slice (`Wave`/`Analysis`/`Envelope`; `Pitch`/`Transposition`/`Formant` report a plain `ProgramError`). Each branch's duration formula was derived from the C source (`make_time_display`, and `structures.h`'s `wlength`/`wanted`/`frametime` field comments) and independently cross-checked, not assumed: the `Analysis` branch's `wlength = insams[0]/channels` formula was confirmed against WP-1.4's own already-established frame count for `capm.wav`/`capm.ana` (`2901528/1026 == 2828` exactly); the `Analysis` branch's `frametime = decimation_factor/original_sample_rate` (computed in `f32`, matching the C struct's field type, before widening to `f64`) and the `Envelope` branch's `secs = samples * window_size * 0.001` were both confirmed to reproduce a live `sndinfo len` run's `%.6lf`-precision output exactly, including one case (`crklenv.evl`) where the naive display-precision value of `window_size` (`2.902494`) was not enough -- the real stored `f32` (`2.9024942...`) was needed to match the legacy output's 6th decimal digit. `format_duration` ports `make_time_display`, including a genuine legacy quirk carried over as-is rather than fixed: `if(mins > 60)`, not `>= 60`, so a duration of exactly 60 minutes prints `"60 mins ..."` rather than rolling over to `"1 hrs 0 mins ..."`; not logged in `LEGACY-BUGS.md` since it is fully deterministic, not the uninitialized-memory kind of bug that file is for. `docs/manual/sounds/clip5-all.wav` (a 37 MB, ~3.5-minute stereo corpus file, not previously used by any `sndinfo` test) was added to the regression suite specifically to exercise `make_time_display`'s `mins` branch and the `Wave` branch's integer-division-by-channel-count with a real stereo (not mono) file; the `hrs` branch remains unconfirmed live, since no corpus file is over an hour long. `CommandSpec::sndinfo_len()` added to `cdp-params`, same shape as `sndinfo_props()`. Wired into `cdp-cli` as `cdp sndinfo len ...` and the existing `sndinfo` legacy-name launcher binary. Verified: 6 in-repo regression tests (the four `props` corpus files plus `clip5-all.wav`); live, via `cdp8-postmerge`, byte-for-byte `diff` of `cdp sndinfo len`/`sndinfo len` against real `legacy` for all five corpus files, a bare `sndinfo len`, a nonexistent infile, and an infile plus a trailing extra word -- all matched exactly. `cargo test --workspace`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. Third slice: `crates/cdp-programs/src/sndinfo/smptime.rs`/`timesmp.rs` port `case(INFO_SAMPTOTIME)`/`case(INFO_TIMETOSAMP)` in `compare.c` (`sndinfo smptime infile samplecnt [-g]` converts a sample count to a duration; `sndinfo timesmp infile time [-g]` is its inverse). Both are `SNDFILES_ONLY` in `ap_sndinfo.c` (`Wave` infiles only, unlike `props`/`len`'s `ALL_FILES`); a real analysis-family infile reports the newly-added `"Application doesn't work with this type of infile."` (`ExitCategory::UsageOnly`, `test_application_validity` in `mainfuncs.c`), confirmed live. Both parameters' valid ranges are not fixed constants: `tklib1.c`'s `set_param_ranges` binds them to the already-open infile's own raw sample count (`smptime`) or duration (`timesmp`), so `CommandSpec::sndinfo_smptime`/`sndinfo_timesmp` take that bound as an argument, the first `CommandSpec` builders in `cdp-params` that need one -- built only after `crates/cdp-programs/src/sndinfo/mod.rs`'s new `open_sound_infile` opens the file, since (unlike every other command ported so far) the range check itself depends on it. Found and fixed two real, confirmed gaps while verifying this, both in already-shipped `cdp-params` code: (1) `check_range` never implemented legacy's `flteq`-tolerance snap-to-boundary (`FLTERR = 0.000002`, `tklib1.c`) before comparing -- invisible until now because every previous `CommandSpec`'s bounds were round literals a real token could land on exactly, but `timesmp`'s dynamic `hi` (an infile's own computed duration, e.g. `44174.0/44100.0`) is not bit-identical to the same value's 6-decimal printed form; `sndinfo timesmp marimba.wav 1.001678` (that file's own duration, confirmed live to succeed) would otherwise be wrongly rejected. `check_range` now snaps the value to `lo`/`hi` within `FLTERR` before comparing, matching `read_param_as_value_or_brkfile_and_check_range`'s own `flteq` checks exactly, and returns the (possibly snapped) value for every caller to store, fixing this for every numeric `ParamType` at once, not just this command's. (2) `get_variant_no`'s own non-dash-token and "letter matches nothing" checks each have two different real code paths with two different legacy categories depending on whether the mode has any option flags at all (`ap->option_cnt`) -- previously only the option-phase (`get_option_no`, `USAGE_ONLY`) side of the first case was reachable (a documented gap in `cdp_core::error::CdpError`'s `From<ParamsError>` impl), and the no-options branch of the second case was entirely unimplemented. `smptime`/`timesmp` (flags empty, one boolean variant `-g`) are the first ported commands to reach both: `ParamsError::UnknownParameterInVariantPhase` (new) and the existing `UnknownParameter` now correctly split by category (confirmed live: `sndinfo smptime infile.wav 44174 extra` prints `"ERROR: INCORRECT USE"`, unlike the option-phase case), and `ParamsError::UnknownFlagNoOptions` (new, `"Unknown flag '-%c'"`, distinct text from both `UnknownFlag` and `UnknownVariantFlag`) covers the second. This also caught and corrected a pre-existing, apparently-never-live-checked test: `distort_repeat_leftover_non_flag_token_is_unknown_parameter` asserted `UnknownParameter`/`UsageOnly` for a trailing non-dash token reached via `distort repeat`'s own variant phase, but a live run shows `"ERROR: INCORRECT USE"` there too -- fixed to expect `UnknownParameterInVariantPhase`, confirmed against `docker run cdp8-postmerge distort repeat infile.wav out.wav 3 -c2 -s1 extra`. `wave_duration_secs` was extracted from `len.rs`'s `format_len` (same formula, now shared with `timesmp`) rather than duplicated. Wired into `cdp-cli` as `cdp sndinfo smptime/timesmp ...` and the existing `sndinfo` legacy-name launcher binary; same bare-subcommand usage-text and `argc<4` greeting rules as `props`/`len`. Verified: 11 in-repo regression tests across both modules (mono and stereo corpus files, grouped and ungrouped, an out-of-range value against each command's own dynamic bound, the new no-options error variants); live, via `cdp8-postmerge`, byte-for-byte `diff` of `cdp`/`sndinfo smptime`/`timesmp` against real `legacy` for: both golden-path corpus files (mono and stereo, grouped and ungrouped), a wrong-filetype infile, a nonexistent infile, a bare sub-command, an infile-only invocation, values out of range in both directions, a value exactly at and just past the infile's own dynamic bound (the `flteq` fix), unparseable tokens, a trailing extra word, an unrecognised flag, and a duplicated `-g` -- all matched exactly, including exit codes and blank-line counts. `cargo test --workspace` (`cdp-params` still 67 unit tests -- this slice corrected one existing test's expected error variant rather than adding a new one; `cdp-programs` now 35 unit tests, up from 24, the 11 new `smptime`/`timesmp` tests), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. Fourth slice: `crates/cdp-programs/src/sndinfo/timediff.rs` ports `case(INFO_TIMEDIFF)` in `compare.c` (`sndinfo timediff infile1 infile2`, absolute duration difference). `INFO_TIMEDIFF` is `TWO_SNDFILES` in `ap_sndinfo.c`, the first command in this crate needing two independently-opened infiles (`dz->infile`/`dz->otherfile`) rather than one, and `set_legal_infile_structure` lets the second file's sample rate and channel count differ from the first's -- confirmed live with `marimba.wav` (44100 Hz mono) against `ws2/tsw1-2nd.aiff` (a different rate and format). This command's argument handling is written by hand rather than through `cdp_params::parse`, unlike every other command ported so far: live legacy validates the two infiles in a strict, interleaved order (infile1 exists, then infile1's kind is checked, then only after both pass does it even try infile2), confirmed live in both directions -- `sndinfo timediff capm.ana nonexistent.wav` reports the infile1-kind error without ever touching infile2, and `sndinfo timediff marimba.wav capm.ana` reports a different, infile2-specific error instead. A generic `infile_count: 2` pre-check, the shape every other multi-infile `cdp_params::CommandSpec` would need, checks both files' existence before either file's kind and would get the first case wrong, so this command's argument handling lives directly in `crates/cdp-cli/src/lib.rs`'s `run_sndinfo_timediff` and `timediff::open_infiles` instead. legacy also uses a genuinely different open-failure message per infile: infile1 reuses `super::open_sound_infile`'s existing `"Can't open file %s to read data."`; infile2 uses `"cannot open input file %s to read data."` instead (`legacy/dev/cdp2k/readfiles.c`'s `open_checktype_getsize_and_compareheader`), confirmed live and ported as observed rather than unified into one text. Found and logged a real legacy bug while verifying this, not reproduced: real legacy segfaults (confirmed live, exit code 139, no output at all) given exactly one infile instead of two, because `handle_extra_infiles` (`mainfuncs.c`) reads a second `argv` slot with no bounds check against the remaining token count -- see `docs/migration/LEGACY-BUGS.md`. This port reports a clean `ParamsError::InsufficientCmdlineParameters` instead, since Rust's own array indexing cannot reach that unsafe read at all. `CommandSpec`/`cdp-params` were not changed by this slice. Wired into `cdp-cli` as `cdp sndinfo timediff ...` and the existing `sndinfo` legacy-name launcher binary. Verified: 6 in-repo regression tests (the confirmed cross-format duration difference, order symmetry, a zero-difference identical-file case, and the three interleaved-order/wrong-text cases above); live, via `cdp8-postmerge`, byte-for-byte `diff` of `sndinfo timediff` against real `legacy` for: two valid infiles, a bare sub-command, one infile, a nonexistent infile in each position, a trailing extra word, identical infiles twice, and both orderings of a wrong-kind infile1/infile2 -- all matched exactly except the deliberately-avoided one-infile crash, which has no real legacy output to match against. `cargo test --workspace` (`cdp-programs` now 41 unit tests, up from 35), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. Fifth slice: `crates/cdp-programs/src/sndinfo/lens.rs`/`sumlen.rs` port `case(INFO_TIMELIST)`/`case(INFO_TIMESUM)` in `compare.c` (`sndinfo lens infile [infile2..]` lists each infile's own duration; `sndinfo sumlen infile infile2 [infile3..] [-ssplicelen]` sums them, subtracting one splice length per infile). Both are `MANY_SNDFILES` (an unbounded infile list, `legacy/dev/cdp2k/mainfuncs.c`'s `count_infiles`), the first commands in this crate needing that rather than a fixed count, so -- like `timediff` -- their argument handling is written by hand instead of through `cdp_params::parse`'s fixed `infile_count`. The key finding this slice turned up, from `set_legal_infile_structure` (`ap_sndinfo.c`): `lens` (`has_otherfile == TRUE`, like `timediff`) opens every infile after the first independently, with no cross-file consistency check at all -- confirmed live, `sndinfo lens marimba.wav clip5-all.wav` (mono then stereo) succeeds, each file printing its own genuinely correct duration (`compare.c`'s `INFO_TIMELIST` loop calls `readhead`/`copy_to_fileptr` per infile, refreshing `dz->infile` to that file's own header). `sumlen` (`has_otherfile == FALSE`), by contrast, goes through the strict, generic property-comparison check every other `has_otherfile == FALSE` multi-infile command shares (`open_checktype_getsize_and_compareheader`/`handle_other_infile`, `readfiles.c`): every infile after the first must match infile 1's sample rate and channel count exactly, or the run fails with `"Incompatible sample-rate/channel-count in input file %s."` (channel-count confirmed live with the same mono/stereo pairing; sample-rate inferred from the same source block, not independently confirmable, since every corpus sound file shares one sample rate, 44100Hz) -- this also means `INFO_TIMESUM`'s own loop reusing `dz->infile`'s (infile 1's) `channels`/`srate` for every term, rather than re-reading each file's own header the way `INFO_TIMELIST` does, produces the same result either way, since the properties are already guaranteed equal by the time it runs. This also caught and corrected a real, pre-existing inaccuracy in `timediff.rs`'s own module doc: its example (`marimba.wav` vs `ws2/tsw1-2nd.aiff`) was credited with confirming that `timediff` (also `has_otherfile == TRUE`) allows differing sample rate/channel count, but those two files in fact share both -- only their container format differs (WAV vs AIFF). Corrected in place, with the real confirming case (`marimba.wav` vs `clip5-all.wav`, mono vs stereo, contrasted against `sumlen`'s rejection of the identical pairing) added as a new regression test. At least two infiles are required, with two different confirmed-live messages depending on why: exactly one token after the sub-command name (`sndinfo lens infile`, not in `usage3`'s `CONTINUE` list, unlike `props`/`len`) reports the plain `cdp_params::ParamsError::InsufficientParameters` ("Insufficient parameters on command line."); two or more tokens that still add up to fewer than two real infiles because one looks like a flag (`sndinfo lens infile -x`) reports a new `cdp_programs::sndinfo::INSUFFICIENT_INFILES` ("Insufficient input files for this process\n") instead, from a different, later check (`count_and_allocate_for_infiles`'s own `dz->infilecnt < 2`). The new shared `cdp_programs::sndinfo::split_infile_tokens` (and its `looks_like_flag` helper) reproduces `count_infiles`'s own unflagged-item test precisely (`*(argv[0])!='-' || !isalpha(argv[0][1])`), not the simpler "starts with `-`" guess first tried: confirmed live that a bare `-` trailing token (`sndinfo lens infile infile2 -`) is *not* flag-shaped by this test (`isalpha('\0')` is false) and is instead opened as a genuine third infile named `-`, failing with the ordinary infile-2+ open error, not a flag-related one -- `sndinfo lens infile infile2 -x` (`x` is alphabetic) correctly stops there. `sumlen`'s `-ssplicelen` flag (range `[0.0, 2000.0]`ms, default `15.0`ms, `legacy/dev/include/sndinfo.h`/`cdp2k/tklib1.c`) is a genuine, fixed-range `cdp_params::CommandSpec` flag, so `sumlen::parse_splice_ms` reuses `cdp_params::parse` directly (`infile_count: 0`, `has_outfile: false`, handed only the tokens left after this slice's own infile-list consumption) rather than hand-rolling flag parsing too -- every error case (out-of-range, unparseable, duplicate, missing value, unrecognised flag) already matched this crate's existing `ParamsError` text exactly, confirmed live. Splice length is subtracted once *per infile*, not once total, confirmed live across three identical-file sums (3 × duration − 3 × splice). Found and fixed a real, pre-existing gap in already-shipped `cdp_params::parse`, surfaced by `lens`'s own zero-flags-zero-variants tail check (the same shape `props`/`len` already use): a leftover token there unconditionally produced `ParamsError::TooManyParameters`, regardless of content, because the check never actually looked at the token -- invisible until now since every zero-flags-zero-variants mode confirmed live before this slice was only ever tested with a plain non-dash trailing word. Confirmed live for both `sndinfo props`/`sndinfo lens`: a `-<letter>` token is actually `"Unknown flag -%c on command line."` (`ParamsError::UnknownFlag`, already existed, just unreachable from this check), and a bare `-` is `"Hanging '-' on command line."` (`ParamsError::HangingDash`, new). The new `cdp_params::classify_trailing_token` implements this and is now reused directly by `lens`'s own hand-rolled trailing-flag check too. Wired into `cdp-cli` as `cdp sndinfo lens/sumlen ...` and the existing `sndinfo` legacy-name launcher binary; same bare-subcommand usage-text and `argc<4` greeting rules as every other `sndinfo` sub-command, with the same one-token exception `timediff` has (see above) rather than `props`/`len`'s. Verified: 21 new in-repo regression tests (9 in `lens`, 11 in `sumlen`, 1 new in `timediff`, plus 2 new in `cdp-params` for the trailing-token fix); live, via a freshly rebuilt `cdp8-postmerge` (the previous image predated this session and was rebuilt from current `HEAD` first, confirming legacy source itself was unchanged since, only moved), byte-for-byte `diff` of `cdp`/`sndinfo lens`/`sumlen` against real `legacy` across 26 cases (bare, one-infile, two/three valid infiles including a stereo one, wrong-kind and nonexistent infiles in every position, an unrecognised flag, a hanging dash, mismatched channel counts for both commands, every `-s` error case, and the legacy-name launcher binary) -- all matched exactly, including exit codes; plus a regression re-check of `props`/`len`/`timediff` against real `legacy` for the trailing-flag/hanging-dash fix. `cargo test --workspace` (`cdp-params` now 69 unit tests, up from 67; `cdp-programs` now 62, up from 41), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. Sixth slice: `crates/cdp-programs/src/sndinfo/units.rs` ports `case(INFO_MUSUNITS)`/`do_musunits` in `musunit.c` (`sndinfo units mode value`, converts between musical units), modes 1 (`MIDI to FRQ`) and 2 (`FRQ to MIDI`) only -- the two purely numeric PITCH-section modes; the rest of the PITCH section (3-6) needs `ap_sndinfo.c`'s `readnote_as_midi` note-name grammar, INTERVAL/SPEED (7-23) need `interval_to_semitones`'s interval-name grammar, and LOUDNESS (24-25) plus six further undocumented DELAY/TEMPO modes (26-31, found live: `sndinfo units 32 1` reports `"Program mode value [32] is out of range [1 - 31]."`, confirming the real mode range is 31, not the 25 `spec/usage/sndinfo/units.txt` documents) are each their own formula -- all deferred. `INFO_MUSUNITS` is `NO_FILE_AT_ALL` in `ap_sndinfo.c`'s `assign_process_logic`, the first `sndinfo` sub-command in this crate with no infile or outfile at all, just a mode number and one value; `cdp_params::CommandSpec::sndinfo_units` is the first builder with `infile_count: 0`. Both modes are a plain `ParamType::Double` (`parstruct.json`'s `INFO_MUSUNITS` entry, confirmed no `legacy_index` mismatch this time, unlike `modify loudness`'s `-l`) whose range is not a fixed literal but mode-dependent (`tklib1.c`'s `set_param_ranges`, `case(INFO_MUSUNITS): switch(mode)`), the same "range is a parameter, not a literal" shape `sndinfo_smptime`/`sndinfo_timesmp` established for an infile-dependent bound -- here it is mode-dependent instead, carried by the new `cdp_programs::sndinfo::units::Mode::range`. A genuine legacy quirk, ported as observed: `do_musunits` always prints its result text once, unconditionally, via `print_outmessage_flush(errstr)`, before returning its exit status -- for a success this is the only print, but mode 2 also has its own internal range check (`frq_to_midi`'s `MINPITCH`/`FRQMAX` literals, `9.0`/`12543`) narrower than the generic `cdp_params` range check at both ends (`8.175799 < 9.0` and `12543 < 12543.853951`, confirmed live with values in each gap), so a value can pass the outer check and fail this inner one, and when that happens `errstr` is printed twice -- once plain, then again with the usual `"ERROR: CANNOT ACHIEVE TASK:"` header. `units::run` reproduces this directly (prints its result text itself, then returns an `Err` carrying the same text when the inner check failed) rather than only ever printing on success the way every other `sndinfo` sub-command ported so far does. Mode 1's equivalent internal check is ported for the same fidelity but confirmed unreachable (its literals exactly equal the outer check's own bounds for that mode). `my_LOG2`'s `CONVERT_LOG10_TO_LOG2` (`3.321928`) is ported as the literal `musunit.c` uses, not a computed `log2`, since the two differ in the low decimal digits this crate's 6-decimal-place live comparisons are sensitive to (needed `#[allow(clippy::approx_constant)]`, the same reason `cdp-dsp`'s `LEGACY_PI` is a literal rather than `std::f64::consts::PI`). Wired into `cdp-cli` as `cdp sndinfo units ...` and the existing `sndinfo` legacy-name launcher binary; same bare-subcommand usage-text shape as every other `sndinfo` sub-command, and the same one-token `argc<4` greeting exception `props`/`len` use (a mode with no value is one token). Verified: 7 in-repo regression tests (both modes' golden values, both range endpoints, the outer/inner range-check gap at both ends, `Mode::range`'s own bounds); live, via `cdp8-postmerge`, byte-for-byte `diff` of `sndinfo units`/`cdp sndinfo units` against real `legacy` across 18 cases -- both modes' golden values, both modes' range endpoints (outer and, for mode 2, inner), values failing the outer check, values in the outer-passes/inner-fails gap at both ends for mode 2, a bare sub-command, mode 0, mode 32, a non-numeric value, a missing value, and a trailing extra token -- all matched exactly, including exit codes and the double-print case. `cargo test --workspace` (`cdp-programs` now 69 unit tests, up from 62), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. Seventh slice: `crates/cdp-programs/src/sndinfo/maxsamp.rs` ports `case(INFO_MAXSAMP)`/`try_header`/`get_and_set_float_maxmin`/`get_and_set_float_maxmina` in `compare.c` (`sndinfo maxsamp infile [-f]`, the largest-magnitude sample in a sound file, or the largest/smallest raw float values in an analysis-family file). `INFO_MAXSAMP` is `ALL_FILES` in `ap_sndinfo.c` (same scope as `props`/`len`) with no output file and one boolean variant `-f` (`parstruct.json`: `param_cnt: 0`, `var_flags: "f"`), confirmed live to share the no-options `ParamsError::UnknownFlagNoOptions` category `smptime`/`timesmp`'s `-g` already established. This is the first `sndinfo` sub-command that reads real sample data rather than only header fields, and the first to have a header-only shortcut at all: legacy first tries a small set of named properties (`maxamp`/`maxpfamp`/`maxloc`/`maxrep` for a sound file; `maxpfamp`/`maxnfamp`/`maxprep`/`maxnrep` for an analysis-family file) that an earlier CDP run may have written into the file, falling back to a real scan only when any of them is missing (`dz->vflag[FORCE_SCAN]`/`-f` always forces the real scan). `cdp_sf::PropertyBlock::get_i32`/`get_f32` (already shipped, generic named-property accessors) needed no changes at all. Confirmed live, and ported as an observed quirk rather than a bug: `docs/manual/sounds/marimba.wav` carries a stale old-format header (`maxamp` property, worth `32767`-normalised `0.928007`) recording its peak at sample 44310, but forcing a real scan (`-f`) finds the same peak *magnitude* recurring earlier, at sample 136, with a different repeat count -- both are correct on their own terms, since `sndinfo maxsamp`'s entire point for a file carrying this header is to trust it verbatim rather than re-scan. This also pinned down `globcon.h`'s `MAXSAMP` as `32767.0`, not `32768.0` (a genuine ambiguity: `columns.h` separately defines a same-named macro as `32767.0` too, but `F_MAXSAMP` differs between the two headers, `1.0` vs `32767.0`) -- confirmed live, dividing marimba's raw `maxamp` property by `32768.0` gives `0.927979`, not the real `0.928007`. Two further legacy quirks ported as observed, neither confirmed live (no corpus file reaches either branch): the sound-file header shortcut's gain calculation divides by the raw `maxamp` int even when only the new-format `maxpfamp` float property is present (in which case that int is still its initial `-1`, so the gain read out for a new-format-only file would be `1.0/-1.0`, not a division by the real peak); and the gain block's own gate differs by one function -- the header shortcut prints it whenever `newmaxamp>=0.0`, the real scan only when `maxamp>0.0f` -- so a genuinely silent (all-zero) file would print a `0.0000`-gain block from a stale header but none at all from a real scan. The analysis-family real scan (`get_and_set_float_maxmina`) has its own confirmed, subtly different "occurs" format string from the header-shortcut branch's (eleven dots and two spaces vs twelve dots and one space) -- both transcribed character-for-character from source, not assumed identical. Wired into `cdp-cli` as `cdp sndinfo maxsamp ...` and the existing `sndinfo` legacy-name launcher binary; same bare-subcommand usage-text and `argc<4` greeting rules as `props`/`len`. Verified: 6 in-repo regression tests (`marimba.wav` header-shortcut and forced-scan, disagreeing as described above; `ws2/tsw1-2nd.aiff` and `clip5-all.wav` -- mono and stereo, both with no header shortcut available -- for the sound-file real scan; `capm.ana`/`crklenv.evl` for the analysis-family real scan); live, via `cdp8-postmerge`, byte-for-byte `diff` of `cdp sndinfo maxsamp`/`sndinfo maxsamp` against real `legacy` across 14 cases -- all five corpus files (marimba both with and without `-f`), a bare sub-command, a nonexistent infile, a trailing extra token, an unrecognised flag, a duplicated `-f`, `-f` with an attached value, and the legacy-name launcher for both the bare and golden-path cases -- all matched exactly, including exit codes. `cargo test --workspace` (`cdp-programs` now 75 unit tests, up from 69), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean.

**Next**: the remaining 9 `sndinfo` sub-commands (several of which, e.g. `findhole`/`prntsnd`, need real sample-data scanning rather than header-only reads, a complexity level `maxsamp` (this row's own seventh slice) has now shown this crate can handle), the rest of `units` (modes 3-31), and `Pitch`/`Transposition`/`Formant` support once a real corpus example of any of them turns up (or is generated); WP-2.2 (`housekeep`) has now also started, in parallel, and does not depend on this slice. |

| 2.2 `housekeep` | in progress | One slice: `housekeep copy 1 infile outfile` (`HOUSE_COPY`, mode `COPYSF`) only, of the thirteen sub-commands `get_process_no` (`legacy/dev/houskeep/ap_house.c`) recognises (`copy`, `remove`, `chans`, `bundle`, `sort`, `respec`, `extract`, `bakup`, `gate`, `disk`, `batchexpand`, `endclicks`, `deglitch`) and mode 2 (`DUPL`, auto-named multiple copies) of `copy` itself. `crates/cdp-programs/src/housekeep/copy.rs` ports `do_duplicates`'s `case(COPYSF)` (`legacy/dev/houskeep/dupl.c`). This is the first `cdp-programs` command that both reads and writes real sample data -- every `sndinfo` sub-command ported so far (WP-2.1) only reads -- and the first genuinely new program wired into `cdp-cli` since WP-1.4/1.5's `pvoc anal`/`synth wave` (which predate WP-2.1 entirely). Scope: `cdp_sf::FileKind::Wave` with `SampleType::Short16` or `Float32` sample data, the only two shapes `cdp_sf::SoundFileWriter` can write; `Analysis`/`Envelope`/`Pitch`/`Transposition`/`Formant` (each its own special-cased branch in legacy: `dz->pitches`/`dz->transpos` for `Pitch`/`Transposition`, a `window_size` copy for `Envelope`, a from-scratch analysis-header write for `Analysis`) report a plain `ProgramError`. Confirmed live, and ported as an observed characteristic of legacy's own copy rather than a bug: it is not a byte-for-byte file copy. The output is always written as `WAVE`, even when infile is `AIFF` (confirmed against `docs/manual/sounds/ws2/tsw1-2nd.aiff`); it always gains a freshly-computed `PEAK` chunk and a fresh `DATE` property (`legacy/dev/cdp2k/mainfuncs.c`'s `create_sized_outfile`/`establish_peak_status`, shared by every `SNDFILE_OUT` program); and it drops every other named property the infile had -- confirmed live, `marimba.wav`'s own `maxamp`/`maxloc`/`maxrep` properties (see WP-2.1's seventh slice, `sndinfo maxsamp`, above) do not survive the copy. Sample data itself is bit-exact, confirmed live via Python's own `wave` module against a real stereo file (`docs/manual/sounds/clashmixtest.wav`) and via a full byte-for-byte `diff` of the entire output file (mono, stereo and AIFF-to-WAVE cases) with only the `DATE` property's and the `PEAK` chunk's own timestamp fields excluded, both legitimately non-deterministic (both stamp the current wall-clock time; two runs of the same command a few seconds apart produce different bytes only there). Also confirmed live, and a real, generic legacy behaviour: every `SNDFILE_OUT` program's `create_sized_outfile` refuses to overwrite an already-existing outfile (`"Cannot open output file %s\n"`, `DATA_ERROR`) -- `cdp_sf::SoundFileWriter::finalize` does not check this itself (it always overwrites via `std::fs::write`), so `copy_once` checks it directly; this same check likely applies to `synth wave`/`pvoc anal` too, neither of which currently enforces it -- a real, pre-existing gap this slice found but did not fix, since those two commands belong to WP-1.4/WP-1.5, not this one. This slice also found and fixed a real, previously-unconfirmed ordering bug in already-shipped `cdp_params::parse` (used by every command in this crate): it checked whether enough total tokens were present for the outfile/params *before* checking whether the infile(s) given actually exist, the opposite order from legacy's own `count_and_allocate_for_infiles` (which always runs first) -- confirmed live, `housekeep copy 1 nonexistent.wav` (missing both a real infile and the outfile) reports the infile-open error, not `"Insufficient cmdline parameters."` `parse` now checks infile existence for as many infile tokens as are present before the total-length check, fixing this for every future command with `infile_count >= 1` and `has_outfile: true` or required params at once; this was unreachable via any already-shipped, live-tested command (`sndinfo`'s commands are either `has_outfile: false` or take no params beyond the infile, and `synth wave`/`pvoc anal` take no infile at all), confirmed safe against every existing `cdp-params` unit test (none exercises a simultaneously-nonexistent-infile-and-too-few-tokens case). Also found, live, but *not* fixed (belongs to WP-1.4/WP-1.5, not this WP): `synth wave`/`pvoc anal`'s own mode-number handling validates the mode *before* checking whether any further tokens are present at all, so `synth wave 99`/`synth wave 0` (mode invalid, zero further tokens) currently report a mode-range error, where real legacy (confirmed live) reports the greeting plus `"Insufficient parameters on command line."` unconditionally whenever argc is this short, before even trying to parse the mode number -- `housekeep copy`'s own dispatch in `cdp-cli` was written to check this correctly from the start (`rest.is_empty()` before `parse_mode`), confirmed live for `housekeep copy 0`/`housekeep copy abc` (an out-of-range and an unparseable mode number, respectively) both producing the greeting-plus-insufficient-parameters response, matching real `legacy` exactly. `cdp_params::CommandSpec::housekeep_copy_once()` added, same zero-params/zero-flags/zero-variants shape as `sndinfo_props()` but with `has_outfile: true`. Wired into `cdp-cli` as `cdp housekeep copy 1 ...` and a new `housekeep` legacy-name launcher binary (`housekeep --version` prints `7.1.1`, confirmed live). Verified: 4 in-repo regression tests (`marimba.wav` round-trips format and sample data exactly and gains a fresh `DATE` while losing `maxamp`; `ws2/tsw1-2nd.aiff` is written as `WAVE` not `AIFF`; refuses to overwrite an existing outfile; an `Analysis`-kind infile reports "not implemented"); live, via `cdp8-postmerge`, byte-for-byte `diff` of `cdp housekeep copy`/`housekeep copy` against real `legacy` for: a bare `housekeep`, a bare `housekeep copy`, mode `1`/`0`/`abc` alone, a nonexistent infile alone and with an outfile given, an out-of-range mode with full arguments, the legacy-name launcher's `--version`, and the golden-path copy itself for three real corpus files (mono `marimba.wav`, mono `AIFF` `tsw1-2nd.aiff`, stereo `clashmixtest.wav`) -- all matched exactly, including exit codes and (modulo the two confirmed-non-deterministic timestamp fields) every output byte. `cargo test --workspace` (`cdp-programs` now 79 unit tests, up from 75), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. Second slice: `crates/cdp-programs/src/housekeep/chans.rs` ports `do_channels`'s `case(HOUSE_CHANNEL)` (`legacy/dev/houskeep/channels.c`, `housekeep chans 1 infile channo`: extracts one channel to a new, auto-named mono file). Refactored `copy.rs`'s three shared helpers (the outfile-exists refusal check, the fresh-`DATE` timestamp, and the whole `SoundFileWriter`/`PEAK`/`DATE` write path) up into `housekeep::mod.rs` as `pub(crate)` functions (`check_outfile_does_not_exist`/`now_unix_timestamp`/`write_wave_file`), so `chans` reuses them rather than duplicating; `copy.rs`'s own behaviour and tests are unchanged by this. Like `copy`, takes no outfile at all -- the output filename is derived from the infile's own path (confirmed live: `housekeep chans 1 marimba.wav 1` writes `marimba_c1.wav`), which legacy calls `dz->wordstor[0]` but is simply the infile's own path string as given on the command line, needing no new "special data" machinery: this crate already has that exact string as `ParsedCommand::infiles[0]`. `channo`'s valid range (`1..=infile.channels`) is infile-dependent, the same "range is a parameter, not a literal" shape `sndinfo_smptime`/`sndinfo_timesmp` (WP-2.1) already established, so `cdp_params::CommandSpec::housekeep_chans_channel(channels)` takes it as an argument, built only after the infile is open (reusing `cdp_programs::sndinfo::open_sound_infile` directly, confirmed live to produce the identical `"Application doesn't work with this type of infile."` text for a non-sound infile here too, not a housekeep-specific message). Confirmed live and ported as observed: an out-of-range `channo` reports the generic `"Parameter[1] Value (...) out of range (1.000000 to N.000000)"`, not the more specific `"There is no channel %d in the input file"` text `param_preprocess` also carries for this mode -- that second check is confirmed unreachable, masked by the generic range check firing first on the exact same condition (tested with a channel number one past a mono file's own count). The output filename's numeric suffix is unpadded (`_c1`, `_c2`, ...), unlike `copy` mode 2's future zero-padded `_001`/`_002` scheme -- both are ported from the same two legacy helper functions (`insert_new_chars_at_filename_end`/`insert_new_number_at_filename_end`, `legacy/dev/cdp2k/mainfuncs.c`) called with different literal prefixes, confirmed by reading both call sites side by side rather than assumed identical. This slice's own `numbered_channel_filename` treats only a `.` occurring after the last path separator as an extension, matching those two functions' own backward-scanning behaviour (they stop at the first `/`/`\`/`:` encountered). Same scope restriction as `copy`: `cdp_sf::FileKind::Wave` with `SampleType::Short16`/`Float32` only; every other file kind or sample type reports a plain `ProgramError`. Wired into `cdp-cli` as `cdp housekeep chans 1 ...` and the existing `housekeep` legacy-name launcher binary; same bare-subcommand usage-text and `argc<4` greeting rules as `copy`, confirmed live including the mode-validation-after-token-count-check ordering `copy`'s own dispatch already got right (`housekeep chans 0`/`housekeep chans abc` both produce the greeting-plus-insufficient-parameters response, not a mode error). Verified: 5 in-repo regression tests (a filename-suffix unit test covering an extension, a directory component, and no extension at all; mono `marimba.wav` channel 1 and stereo `clashmixtest.wav` channel 2, each checked against the real corresponding samples of the real infile; refuses to overwrite an existing outfile; an `Analysis`-kind infile reports "not implemented"); live, via `cdp8-postmerge`, byte-for-byte `diff` of `cdp housekeep chans`/`housekeep chans` against real `legacy` for: a bare `housekeep chans`, mode `1` alone, mode `1` with only an infile, mode `0`/`6` (below and above range), an out-of-range/zero/unparseable `channo`, a trailing extra token, a nonexistent infile, a wrong-kind (`Analysis`) infile, and the golden-path extraction itself for both a mono and a stereo real corpus file -- all matched exactly, including exit codes and (for the two golden-path output files) every byte with zero exceptions (this slice's two runs happened to land in the same wall-clock second as legacy's, so even the `DATE`/`PEAK` timestamp fields matched, though that is not guaranteed run to run). `cargo test --workspace` (`cdp-programs` now 84 unit tests, up from 79), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. **Next**: `copy` mode 2 (`DUPL`) and `remove` both need a new "special_data" (`SNDFILENAME`) argument-reading mechanism this crate does not model yet (the target filename is never opened as a real file, just captured as a string ahead of normal parameter parsing) -- a natural next slice once that mechanism exists; `chans` modes 2-5, and the ten other `housekeep` sub-commands (`bundle`, `sort`, `respec`, `extract`, `bakup`, `gate`, `disk`, `batchexpand`, `endclicks`, `deglitch`) remain, several (`sort`, `batchexpand`) needing textfile-list grammars this crate also does not have yet; `disk` reports genuinely non-deterministic, environment-dependent free-disk-space figures, which the golden byte-for-byte live-comparison methodology this crate relies on cannot verify the same way. Third slice: added `chans` mode 5 (`MTOS`, `housekeep chans 5 infile outfile`: doubles a mono infile's samples into a stereo outfile) to the same `chans.rs`. A real infile channel count of 2 reports `"This file is already stereo!!"` (`GOAL_FAILED`, confirmed live); any other count reports `"This process does not work with multichannel files!!"`, ported from source but not confirmed live (no corpus file has more than 2 channels). This slice found a second, real legacy quirk: `MTOS`'s own outfile-creation path (the plain `sndcreat_formatted` branch, not the `WAVE_EX` branch `dz->outfile->channels > 2` would select) checks `if(dz->ofd < 0) return DATA_ERROR;` with no `sprintf(errstr,...)` call on that path at all, so an already-existing outfile reports a bare `"ERROR: INVALID DATA"` header with an *empty* detail message -- confirmed live, byte-for-byte (`cat -A`), genuinely different from `copy`/`chans` mode 1's own `"Cannot open output file %s\n"` overwrite-refusal text, so `mono_to_stereo` checks this itself rather than reusing `check_outfile_does_not_exist`. This slice also found a second, more consequential discrepancy in this crate's own model: `ap_house.c`'s `assign_process_logic` classifies `MTOS` as `UNEQUAL_SNDFILE`, the same category `distort repeat` (WP-1.3) is confirmed to use, but confirmed live, `housekeep chans 5 infile` (missing the outfile) reports `"Insufficient cmdline parameters."` -- the `EQUAL_SNDFILE`-shaped text `cdp_params::CommandSpec::unequal_sndfile: false` produces -- not `"Insufficient parameters on command line."` the way `distort repeat`'s own confirmed `UNEQUAL_SNDFILE` case does. Reading `legacy/dev/cdp2k/mainfuncs.c`/`readdata.c` directly traced why: `"Insufficient parameters on command line."` (`readdata.c`'s `read_parameters_and_flags`) fires only when the mode has `ap->param_cnt > 0` (required params beyond infile/outfile) and zero tokens remain once infiles+outfile are consumed -- a check with nothing to do with `EQUAL_SNDFILE`/`UNEQUAL_SNDFILE` at all -- while a missing *outfile* specifically routes through an earlier, separate check this crate has not yet traced to its exact source. `cdp_params::CommandSpec::unequal_sndfile` is confirmed, by this direct counterexample, not to be a faithful general model of legacy's real `EQUAL_SNDFILE`/`UNEQUAL_SNDFILE` distinction (which routes through `count_infiles`, a variable-infile-count mechanism this crate does not implement generally -- see that field's own doc); `housekeep_chans_mtos()` sets it to `false` because that is the value confirmed live for this specific command, with a doc note explaining the real classification and the discrepancy, rather than attempting the deeper parser fix this finding motivates (out of scope for this slice; a candidate for its own future WP-1.3 revisit). Verified: 3 new in-repo regression tests (doubles `marimba.wav` into both output channels exactly; rejects a real stereo infile with the confirmed `GOAL_FAILED` text; the confirmed empty-message overwrite refusal); live, via `cdp8-postmerge`, byte-for-byte `diff` against real `legacy` for: a missing outfile, a stereo infile, a trailing extra token, a nonexistent infile, an already-existing outfile, and the golden-path mono-to-stereo doubling itself -- all matched exactly, including exit codes and every output byte. `cargo test --workspace` (`cdp-programs` now 87 unit tests, up from 84), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. Fourth slice: a bug fix, found while checking `chans` mode 4's own stdout, not a new sub-command. `copy` mode 1 and `chans` mode 5 were both missing a real piece of legacy output: `display_virtual_time` (`legacy/dev/cdp2k/writedata.c`), called from `write_exact_samps`/`write_samps` (both already-used write paths) after every buffer of samples written, prints `"\r%d min %5.2lf sec"` with no trailing newline -- confirmed live, both `housekeep copy 1` and `housekeep chans 5` print this and this crate's ports of both printed nothing at all, a real, previously-undetected gap in both already-merged slices (their own golden-path verification only ever diffed the output *file*, never stdout, for a successful run -- only error-path stdout was ever compared byte-for-byte). The divisor differs by case, both confirmed live: `COPYSF` divides by the infile's own channels (`secs = samps_sent/(infile->srate*infile->channels)`, matching the file's real duration exactly, `"\r0 min  1.00 sec"` for `marimba.wav`); `MTOS`'s own `SNDFILE_OUT` case uses the same formula but with the *interleaved stereo* sample count written over the *mono* infile's channel count as the divisor, confirmed live to report exactly double the file's real duration (`"\r0 min  2.00 sec"` for the same file). This also surfaced that `display_virtual_time` is called once per internal write-buffer, not once per command -- confirmed live, a larger infile (`clashmixtest.wav`, 2.49s, via `chans` mode 4) prints several `\r`-separated ticks with increasing values -- and that buffer's size (`create_sndbufs`'s `bigbufsize = Malloc(-1)`, `legacy/dev/cdp2k/tklib3.c`) is the largest available block of free memory at allocation time, not a fixed constant, so the exact tick count is environment-dependent and not reproducible in general, the same fundamental non-determinism `disk`'s own free-space report has. The new shared `cdp_programs::housekeep::print_virtual_time` (and its pure, unit-tested `format_virtual_time` helper) is therefore called exactly once per command, with the final, complete sample count, deliberately approximating legacy's *last* tick (the only one that would remain visible in a real terminal, since each `\r` overwrites the previous one); every corpus file this crate's tests use is small enough that real legacy also only emits one tick for it, confirmed live, so this simplification is not observable for any case this crate's own tests exercise. Both `copy_once`/`mono_to_stereo` now call it. Verified: 2 new in-repo unit tests for `format_virtual_time` (matching real legacy's printed text for the corpus files' own real durations, and a minutes-rollover case not reachable by any corpus file); live, via `cdp8-postmerge`, byte-for-byte `diff` of the complete stdout (not just the output file) for `housekeep copy 1` against both `marimba.wav` and `ws2/tsw1-2nd.aiff`, and `housekeep chans 5` against `marimba.wav` -- all matched exactly, including the leading `\r` and the unconditional tail. `cargo test --workspace` (`cdp-programs` now 89 unit tests, up from 87), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. **Next**: `chans` modes 2-4 remain (mode 4, `STOM`/mix-down-to-mono, was the case that surfaced this slice's own bug, so its own port should reuse `print_virtual_time` from the start); `copy` mode 2 and `remove` still need the "special_data"/`SNDFILENAME` mechanism noted above. |

| 2.2 `housekeep` (cont'd) | in progress | Fifth slice: added `chans` mode 4 (`STOM`, `housekeep chans 4 infile outfile [-p]`: mixes a stereo infile down to mono, `-p` takes the half-difference instead of the half-sum) to `chans.rs`, reusing `print_virtual_time` from the start as planned. A mono infile reports `"This file is already mono!!"` (`GOAL_FAILED`, confirmed live); more than two channels would take `STOM`'s own "thumbnail" two-pass normalising downmix, not ported (a real port gap, reported as `ProgramError`, not the unconfirmed source text) since no corpus file has more than two channels. The same empty-message overwrite-refusal quirk `mono_to_stereo` (fourth slice) already found applies here too, confirmed live. This slice's own live verification surfaced a second, genuinely more significant bug, this time in `cdp-sf` itself, not `cdp-programs`: `writer.rs`'s `encode_pcm16` computed `(sample * MAXSHORT).round()` entirely in `f32`, but legacy's own equivalent (`fputfloatEx`'s `(short)cdp_round(*fp*MAXSHORT)`, `legacy/dev/newsfsys/snd.c`) computes this in `f64` -- `MAXSHORT` is an unsuffixed C floating literal (`(32767.0)`, `double` by default) and `cdp_round` takes a `double` argument, so the sample promotes to `double` before the multiply, and the whole multiply-and-round happens in double precision. This is not a theoretical distinction: `housekeep chans 4`'s real stereo average of `docs/manual/sounds/clashmixtest.wav` lands a value that, multiplied by `MAXSHORT` in `f32`, comes out exactly `0.5` (rounding up to `1`), but the identical value multiplied in `f64` comes out `0.4999999995...` (rounding down to `0`, real legacy's own output) -- confirmed by a standalone Rust program computing both paths bit-for-bit, not inferred from documentation. This is `cdp-sf`'s own bug, shared by every command that writes 16-bit PCM output, not specific to `chans` mode 4; it was invisible until now because no earlier command's own regression tests or live comparisons happened to exercise a sample landing exactly on a rounding boundary (`copy`/`chans` modes 1/5 only ever copy or duplicate samples verbatim, never compute a new value through arithmetic the way an averaging mixdown does). Fixed directly in `crates/cdp-sf/src/writer.rs`'s `encode_pcm16` (cast to `f64` before multiplying), confirmed to change nothing for every already-passing test and live comparison in the whole workspace (a fix that only changes behavior exactly at a rounding boundary, which nothing else this crate has shipped yet lands on). `cdp_params::CommandSpec::housekeep_chans_stom()` added: same zero-params shape as `housekeep_chans_mtos()` plus one boolean variant, `-p` (`CHAN_INVERT_PHASE`), same `unequal_sndfile: false`-despite-`UNEQUAL_SNDFILE`-classification caveat as `mtos`'s own doc, confirmed live the same way (`housekeep chans 4 infile` alone reports `"Insufficient cmdline parameters."`). Verified: 4 new in-repo regression tests (the stereo average and its `-p` half-difference, both checked against the infile's own real samples with the same `f64`-precision 16-bit quantisation `encode_pcm16` now applies; a mono infile rejected; the empty-message overwrite refusal); live, via `cdp8-postmerge`, byte-for-byte `diff` against real `legacy` for: a mono infile, a missing outfile, a trailing extra token, an unrecognised flag, a duplicated `-p`, a nonexistent infile, an already-existing outfile, and the golden-path mixdown itself both with and without `-p` -- all matched exactly, including exit codes and (after the `encode_pcm16` fix) every output byte with zero exceptions in either golden-path file. `cargo test --workspace` (`cdp-programs` now 93 unit tests, up from 89), `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets -- -D warnings` all clean. **Next**: `chans` modes 2-3 remain; `copy` mode 2 and `remove` still need the "special_data"/`SNDFILENAME` mechanism; the other ten `housekeep` sub-commands (`bundle`, `sort`, `respec`, `extract`, `bakup`, `gate`, `disk`, `batchexpand`, `endclicks`, `deglitch`) are all still unstarted. |

## Programs
