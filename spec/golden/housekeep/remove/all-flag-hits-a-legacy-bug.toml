description = "the -a flag, which legacy documents but which aborts inside legacy itself (see LEGACY-BUGS.md)"
program = "housekeep"
subcommand = "remove"
argv = [
    "remove",
    "in.wav",
    "-a",
]
known_deviation = "Legacy aborts with the same internal error every other form hits (see LEGACY-BUGS.md), so the -a flag has no observable legacy behavior to match. The port is also still missing the flag, which is a separate phase R gap."

[[inputs]]
name = "in.wav"
from = "docs/manual/sounds/marimba.wav"

[expected]
exit_code = 255
stdout = """
ERROR: INTERNAL ERROR: (Bug?)
ERROR: Invalid process_type 20: create_sized_outfile()


"""
stderr = ""

[expected.outputs]
