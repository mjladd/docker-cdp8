description = "a threshold high enough to find a real hole, so the algorithm itself is exercised rather than only the zero-threshold shortcut"
program = "sndinfo"
subcommand = "findhole"
argv = [
    "findhole",
    "in.wav",
    "-t0.5",
]

[[inputs]]
name = "in.wav"
from = "docs/manual/sounds/marimba.wav"

[expected]
exit_code = 0
stdout = """
Maximum holesize is 0.003696 at time 0.020476


"""
stderr = ""

[expected.outputs]
